plugins {
    id 'java'
    id 'idea'
    id 'eclipse'
    id "com.qixalite.spongestart" version "1.4.3"
}

group= 'us.illyohs.bansdb' // http://maven.apache.org/guides/mini/guide-naming-conventions.html
archivesBaseName = 'bansdb'
version = '0.0.1'

// In this section you declare where to find the dependencies of your project
repositories {
    jcenter()
    maven {
        name = 'sponge'
        url = 'http://repo.spongepowered.org/maven'
    }
}

jar {
    manifest {
        attributes(
                'Built-By': System.properties['user.name'],
                'Created-By': "${System.properties['java.vm.version']} (${System.properties['java.vm.vendor']})",

                'Implementation-Title': project.name,
                'Implementation-Vendor': "https://github.com/DragonTechMC"
        )
    }
}

spongestart{
    eula true

    //optional configs
    spongeVanillaBuild 'LATEST'
    spongeForgeBuild 'LATEST'
}

ependencies {
    compile 'org.spongepowered:spongeapi:4.0.3'
}